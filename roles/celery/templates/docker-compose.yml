version: '3'

services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery worker -A config.celery -b {{ RABBITMQ_URL }} --loglevel=info -Q packages,default --hostname=packages01@%h
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.local
